import 'package:flutter/material.dart';

class OfbizForm extends StatelessWidget {
  const OfbizForm({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow,
    );
  }
}
